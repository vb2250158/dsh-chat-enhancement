# 推理结束后延迟折叠

自动展开的推理行在首次观察到推理结束后等待 3 秒，再调用原展开控件折叠。配置 `chat-enhancement.reasoningCollapseDelayMs` 默认为 `3000`，允许非负整数毫秒；界面说明使用实际配置值。

每行最多持有一个计时器，重复 DOM 更新不重置计时；恢复推理时取消旧计时，下一次结束重新计时。移除行或卸载控制器时清理计时器。关闭功能仍立即折叠自动展开的行，用户原先手动展开的行不受影响。

假时钟测试覆盖 2999 毫秒仍展开、3000 毫秒折叠、重复更新、再次推理、行移除、卸载、手动展开和关闭功能。
