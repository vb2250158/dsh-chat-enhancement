# 运行中的实际模型

运行标签挂在公开 `conversation.chat.running-status` 槽，显示当前运行轮最近一次模型请求的 `requestConfig`，目录只解析显示名称。没有本轮请求记录时不借用上一轮，也不读取输入框的下一轮选择。工具执行阶段保留最近一次实际请求。

重定向提示读取 `runningModelRedirect` 日志投影，只有轮次、步骤和目标均匹配时才显示“已重定向”。完整来源与目标放在悬停提示中。运行结束后宿主卸载该槽，已完成消息继续使用各自的历史记录。

宿主只增加显示槽，不包含模型或重定向业务逻辑。没有该槽的宿主不会渲染运行标签。
